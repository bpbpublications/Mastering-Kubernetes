apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restored-pvc
spec:
  dataSource:
    name: db-snapshot
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  storageClassName: csi-storage-class
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi