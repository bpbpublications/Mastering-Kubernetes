apiVersion: v1
kind: Pod
metadata:
  name: configmap-volume
spec:
  containers:
  - name: web-container
    image: nginx
    volumeMounts:
    - name: config-volume
      mountPath: /etc/config
  volumes:
  - name: config-volume
    configMap:
      name: env-config
