apiVersion: batch/v1
kind: Job
metadata:
  name: hellojob
spec:
  template:
    spec:
      containers:
      - name: hello
        image: busybox
        command: ["echo", "Hello from a Kubernetes Job"]
      restartPolicy: Never
  backoffLimit: 3
